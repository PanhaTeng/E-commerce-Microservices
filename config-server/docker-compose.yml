version: "3.7"
services:
  config-server:
    container_name: config-server
    image: config-server
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"
    restart: unless-stopped